syntax = "proto2";

package moz.msg;

// This message defines the interface for messages sent from Navigation tools on mosaic to an
// autonomy stack.
message AutonomyNavigation {
  required float latitude = 1;   // Latitude degree of the destination.
  required float longitude = 2;  // Longitude degree of the destination.
}
