syntax = "proto2";

package moz.msg;

import "onboard/signals/common_types/si_units.proto";

message VehicleSpeed {
  /// Vehicle velocity.
  /// The forward direction is positive.
  required MeterPerSecond vehicle_velocity = 1;
};
